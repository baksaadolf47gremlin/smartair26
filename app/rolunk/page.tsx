import { ComingSoon } from '@/components/ComingSoon';

export const metadata = {
  title: 'Rólunk — SMARTAIR',
};

export default function RolunkPage() {
  return <ComingSoon pageTitle="Rólunk" />;
}
